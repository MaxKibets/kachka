# Open Questions

A live tracker for unresolved decisions across the project. Each row points at the doc that
covers the topic, when such a doc exists. Status values look like "Open, non-blocking" or
"Open, blocking `<what>`".

| Question | Status | Related doc |
| --- | --- | --- |
| Illustration style for History's empty-state icon | Open, non-blocking | — |
| System exercise seed list sourcing (wger catalog vs. an in-house list) | Open, non-blocking | — |
| App-wide data model / entity schema, not yet formalized — blocks the exact backup JSON structure | Open, blocking backup implementation | — |
