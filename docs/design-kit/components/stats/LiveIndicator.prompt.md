The "this session is live" tell — a small pulsing accent dot next to the elapsed-time readout, centered under the screen title.

```jsx
<LiveIndicator time="22:08" />
```

The pulse (`wt-pulse`, 1.6s) is reserved for genuinely live/ticking state — don't apply it decoratively elsewhere.
