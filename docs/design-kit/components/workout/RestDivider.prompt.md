Sits between two exercise blocks inside a superset `ExerciseCard`, labeling the rest interval between them.

```jsx
<RestDivider label="90s rest" />
```

Always flanked by two flex-1 hairlines. Not a full-screen rest state — that's `RestBar`.
