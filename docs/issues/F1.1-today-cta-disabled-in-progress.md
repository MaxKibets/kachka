# F1.1 — Today CTAs not disabled while in-progress banner shows

- **Priority:** P1
- **Effort:** S
- **Type:** Spec gap (mockup)
- **Source:** design-review 2026-05-23 (F1.1)

## Affected wireframes

- `docs/wireframes/today-in-progress.html`

## Problem

While the in-progress banner is present, `Repeat last` / `Choose from history` /
`Start blank` must be **disabled** (muted, non-tappable) — the only paths are
Resume / Discard. In `today-in-progress` the amber `Repeat last` is fully bright
and the links look live, silently dropping the "one active workout" guard.

## Spec reference

`docs/spec/today.md` §3.1.c — the decision is already locked; this is a mockup
gap, not a spec decision.

## Action

Render `Repeat last` (amber CTA) and the two text links in a disabled state
(muted, non-tappable) when the in-progress banner is shown. Leave Resume /
Discard as the only active paths.

## Done when

- In `today-in-progress`, the three start-CTAs are visibly disabled.
- Resume / Discard remain the only tappable actions.

## Resolution

✅ Fixed in `today-in-progress.html`. The `Repeat last` card and the two text
links (`Choose from history`, `Start blank workout`) now render disabled
(`opacity: 0.4; pointer-events: none`, `aria-disabled`) while the in-progress
banner is shown; Resume / Discard stay the only tappable actions. Matches the
locked decision in `spec/today.md` §3.1.c — no spec change needed. The stale
"TBD" annotation about CTA behavior was replaced with the locked rule.
